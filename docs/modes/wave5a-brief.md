First read /tmp/claude-0/-home-user-nani-jo-ghar/34ca1c8b-33c7-5b18-84a0-e3a4b149942e/scratchpad/wave3-common.md (codebase map, rules, flock test lock, --canvas for day runs). Your COOK_TEST_PORT=8970. You're on "Cook with Nani" (a worktree based on `claude/funny-fermi-vyrabn`, with Waves 1–4 merged). **Commit early and often: the account may hit its usage limit and you may be resumed.** Another agent (Wave 5B) is changing level-1 content and chop mechanics; you own the UI: `js/cook/ui.js`, `js/cook/flow.js`, `js/cook/order.js`, `css/cook.css`, `cook.html` and UI text in data.

The owner played the build: "hard to know what to do; lots of information all at once." Redesign for clarity and calm:
1. **Intro order card:** when a customer orders, a card appears in the centre of the play area (briefly covering it is fine here, as nothing is live yet) with the customer's face and the order as a sequence list, while the order is spoken. When the player taps (or after the speech plus a short pause), it animates down into the sidebar as the compact order card. A replay button on the compact card can re-open it.
2. **Every order is a sequence list:** one line and one dot per item, always (for clarity and looks). "Any order" items are shown subtly (e.g. no connector line between them), not grouped on one dot. Keep all the leak rules (hidden words as •••, 👁 reveal costs the no-help star, no pictures in rows).
3. **Remove the English step pills** (Water, Tea, Boil…) completely.
4. **Help is a single "?" button**, which pops out the goal/how-to when pressed. The goal text isn't shown permanently. The first-time ghost finger demo stays on the object.
5. **Nani says less:** at the start of every station, a few seconds of silence (no lines, no hints) so the player can work it out; hints only after a longer hesitation; one line at a time, short; fewer "pass me" moments at level 1 (none in the first order of a session). Keep the recasts after mistakes, but short.
6. **Sidebar space and readability:** design the sidebar so the text fits without shrinking the play area. Order card on top (compact, readable at 915x375 and 1366x768), then Nani's current line (one short line, max 2 rows), nothing else permanently. Font sizes readable for a 5-year-old's parent; no truncation, no horizontal scroll. Check every station at all six viewports.
7. **Remove the coins/stars counter** at the top of the sidebar; show pocket money on the day summary and title screen instead.
8. **Result card:** replace "they asked / you did" with a **word review**: each Kutchi word used in this order as a word pill (speaker, Kutchi, and English beside it), marking the words the player got wrong or needed help with; plus the stars and one "next time" tip per missed star (keep those).
Update the test harness for the new UI. Tests (under the lock, one at a time): `--lab` on laptop, phone (flip5-landscape) and ipad-portrait; `--days 2 --canvas --viewport laptop`; `--open-kitchen 2 --viewport laptop`. Look at the screenshots and fix anything cramped. No console errors. Final commit message ends with exactly:
Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Sg4oxYfpd9Pc6xU9yTdNGv
Don't push. Reply in under 200 words, with the paths of 3 representative screenshots (the intro card, a station with the sidebar at phone size, the result card).
